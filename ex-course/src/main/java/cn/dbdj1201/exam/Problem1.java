package cn.dbdj1201.exam;

import lombok.extern.slf4j.Slf4j;

import java.io.*;

/**
 * @author yz1201
 * @date 2020-06-19 13:27
 **/
@Slf4j(topic = "c.Problem1")
public class Problem1 {
    public static void main(String[] args) throws IOException {
        //4, BufferedReader br = new BufferedReader(new FileReader("stu.txt")); 传Reader的实例

        //9, while((line = br.readLine())!=null)  readLine方法,因为是用字符串接收的。

        //21, return o2.getScore()-o1.getScore(); 降序排列

        //25, BufferedWriter bw = new BufferedWriter(new FileWriter("stu.txt",true)); 追加而非覆盖

        //30, bw.write(String.valueOf(list.get(i).getScore())); 转成字符串再写。
    }
}
